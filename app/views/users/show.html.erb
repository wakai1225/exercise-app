<h1>ID:<%="#{@user.patient_id}"%>さんのマイページ</h1>
   <%= link_to "logout", destroy_user_session_path, method: :delete, class: "nav-send" %>
   <%= link_to "登録画面", exercises_path(current_user.id), class: "nav-send" %>
   <%#= link_to "削除", user_path(@exercises), method: :delete, class: "nav-send"%>

<div class="UrlIndex">
  <div class="exercises">
  <% @exercises.each do |exercise| %>
      <iframe src=<%= exercise.url1 %>></iframe>
      <iframe src=<%= exercise.url2 %>></iframe>
      <iframe src=<%= exercise.url3 %>></iframe>
      <iframe src=<%= exercise.url4 %>></iframe>
   <% end %>

  </div>
</div>
  <div class="qrblock">
  <button id="getqrInfo" class="btn btn-default">QRコードを生成</button>
 
  <div id="qrOutput">
    <div><img id="newImg"></div>
  </div>
 </div>